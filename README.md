# Telegram Music Userbot

Современный Python-based Telegram Music Userbot с веб-интерфейсом для управления музыкой в голосовых чатах Telegram.

## 🚀 Возможности

- **Веб-интерфейс**: Современный Bootstrap UI для настройки и мониторинга
- **Музыкальные команды**: `.play`, `.join`, `.stop`, `.pause`, `.resume` и другие
- **Загрузка аудио**: Поддержка YouTube и других источников через yt-dlp
- **Распознавание музыки**: Shazam API для определения треков
- **Голосовые чаты**: Присоединение к голосовым чатам и воспроизведение музыки
- **Безопасность**: Bearer токен аутентификация

## 📁 Структура проекта

```
telegram-music-userbot/
├── core/                    # Основная логика
│   ├── client.py           # Telegram клиент
│   ├── database.py         # Хранение настроек
│   └── music.py            # Музыкальный менеджер
├── web/                     # Веб-сервер
│   └── server.py           # aiohttp сервер
├── templates/               # HTML шаблоны
│   ├── base.html
│   ├── index.html
│   └── login.html
├── static/                  # CSS/JS ресурсы
│   ├── style.css
│   └── script.js
├── run.py                   # Точка входа
├── requirements.txt         # Python зависимости
└── README.md               # Этот файл
```

## 🛠 Установка

### Требования
- Python 3.11+
- ffmpeg
- Telegram API credentials

### Установка зависимостей

```bash
# Системные зависимости (Ubuntu/Debian)
sudo apt update && sudo apt install -y ffmpeg

# Python зависимости
pip install -r requirements.txt
```

### Настройка Telegram API

1. Перейдите на https://my.telegram.org/apps
2. Войдите в свой аккаунт Telegram
3. Создайте новое приложение
4. Получите `api_id` и `api_hash`

## 🚀 Запуск

```bash
python run.py
```

Откройте браузер и перейдите на `http://localhost:5000`

### Переменные окружения

- `AUTH_SECRET` - Bearer токен для доступа к веб-интерфейсу (генерируется автоматически)
- `PORT` - Порт веб-сервера (по умолчанию: 5000)

## 📱 Команды бота

| Команда | Описание |
|---------|----------|
| `.join` | Присоединиться к голосовому чату |
| `.leave` | Покинуть голосовой чат |
| `.play <url>` | Воспроизвести музыку из URL |
| `.stop` | Остановить воспроизведение |
| `.pause` | Поставить на паузу |
| `.resume` | Возобновить воспроизведение |
| `.mute` | Отключить звук |
| `.unmute` | Включить звук |
| `.replay` | Повторить текущий трек |
| `.shazam` | Распознать музыку (ответ на аудио) |
| `.status` | Показать статус бота |

## ⚙️ Конфигурация

### Первоначальная настройка

1. Запустите приложение
2. Откройте веб-интерфейс
3. Введите Auth Secret (смотрите в логах)
4. Введите Telegram API credentials
5. Подтвердите номер телефона
6. Введите код подтверждения (и 2FA пароль при необходимости)

### Голосовые чаты

⚠️ **Примечание**: Функции голосовых чатов могут быть ограничены из-за совместимости с pytgcalls. Веб-интерфейс и базовые функции Telegram работают полностью.

## 🐛 Устранение неполадок

### UPDATE_APP_TO_LOGIN Error
Эта ошибка исправлена в telethon 1.41.2+. Убедитесь, что используете последнюю версию:

```bash
pip install --upgrade telethon
```

### Проблемы с pytgcalls
Если возникают проблемы с голосовыми функциями, проверьте:

```bash
pip install tgcalls
```

### Логи
Логи приложения содержат полезную информацию для отладки. Проверьте консоль при запуске.

## 🔧 Разработка

### Локальная разработка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd telegram-music-userbot

# Установить зависимости
pip install -r requirements.txt

# Запустить в режиме разработки
python run.py --port 8000
```

### Deployment

Проект поддерживает различные платформы:
- Render (см. `render.yaml`)
- Replit
- VPS/Cloud servers

## 📄 Лицензия

Этот проект предназначен для образовательных целей. Используйте ответственно и в соответствии с Terms of Service Telegram.

## 🤝 Вклад в проект

1. Fork репозиторий
2. Создайте feature branch
3. Сделайте изменения
4. Создайте Pull Request

## ⚠️ Предупреждения

- Используйте собственный Telegram аккаунт на свой страх и риск
- Не используйте для спама или нарушения ToS Telegram
- Этот бот предназначен для личного использования в собственных чатах

## 📞 Поддержка

Если у вас есть вопросы или проблемы, создайте Issue в GitHub репозитории.